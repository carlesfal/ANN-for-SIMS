# Marks the python/ directory as a pytest root.
# Run all Python tests with:  python3 -m pytest  (from backend/python/)
# Or a single file:           python3 -m pytest tests/test_utils.py -v
[pytest]
testpaths = tests
